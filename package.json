{
  "name": "block-agreements",
  "version": "1.0.0",
  "private": true,
  "description": "Yah Blocks agreements + match-report share pages (static + Vercel edge OG).",
  "dependencies": {
    "@vercel/og": "^0.6.4"
  }
}
